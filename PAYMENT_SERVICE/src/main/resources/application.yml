spring:
  application:
    name: payment-service

  datasource:
    url: jdbc:mysql://localhost:3307/payment_db?createDatabaseIfNotExist=true
    username: ${DB_USERNAME:root}
    password: ${DB_PASSWORD:Sahil@2003}
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
        format_sql: true

  # KAFKA CONFIGURATION (CRITICAL FOR EVENTS)
  kafka:
    bootstrap-servers: localhost:9092
    producer:
      key-serializer: org.apache.kafka.common.serialization.StringSerializer
      value-serializer: org.springframework.kafka.support.serializer.JsonSerializer
      properties:
        spring.json.add.type.headers: false

# Razorpay Configuration
razorpay:
  key-id: rzp_test_SAoyRXGgKmMxkB
  key-secret: 9hfqd1ZsRdEnxHajARvZ5d5z

# Stripe Configuration (Optional)
stripe:
  api-key: ${STRIPE_API_KEY:sk_test_dummy}

server:
  port: 8083
  servlet:
    context-path: /

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics,prometheus
  endpoint:
    health:
      show-details: always

logging:
  level:
    root: INFO
    com.payment: DEBUG
    org.springframework.web: INFO
